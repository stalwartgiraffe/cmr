[GLOBAL]
# Pin the Pants version for all developers on your project.
pants_version = "2.27.0" # Use your desired Pants version

[anonymous-telemetry]
# Optional: disable telemetry.
enabled = false

[backend_packages]
# Activate the experimental Go backend.
# The `experimental` namespace means that APIs may change in a future version.
backend_packages = ["pants.backend.experimental.go"]

[golang]
# The minimum Go version the distribution discovered by Pants must support.
# Pants will search for a Go binary that is at least this version.
minimum_expected_version = "1.22"


